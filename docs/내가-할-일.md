# 내가 할 일 (이것만 하면 됩니다)

> 시스템·콘텐츠·대시보드·자동화는 전부 완성돼 있습니다.
> 아래는 **계정 주인만 할 수 있는 일**이라 남겨둔 것들이에요. 위에서부터 순서대로.
> 각 단계 상세 절차는 `docs/오픈-가이드.md`, 토큰 발급 화면별 안내는
> `.claude/skills/sns-autopost/references/setup-guide.md`에 있습니다.

## 오픈까지 (한 번만 하면 끝)

- [ ] **1. 브랜치 머지** — GitHub에서 `claude/sns-automation-system-qhd0ip` → `main` 머지 (5분)
- [ ] **2. Pages 켜기** — Settings → Pages → Source = **GitHub Actions** (2분, 저장소는 public 유지)
- [ ] **3. 인스타그램 준비** — 동성인쇄소 계정을 프로페셔널로 전환 + Facebook 페이지 연결 (10분, 휴대폰에서 가능)
- [ ] **4. Meta 토큰 발급** — setup-guide 1번 절차 → 시크릿 `META_ACCESS_TOKEN`, `IG_USER_ID__DONGSUNG_PRINT` 등록 (30~60분)
  - ID를 모르겠으면: 토큰만 등록하고 `npm run doctor` 실행 → 후보 ID를 찾아서 알려줍니다
- [ ] **5. Cloudinary 가입** — 무료 가입 → Dashboard의 `CLOUDINARY_URL` 복사 → 시크릿 등록 (10분)
- [ ] **6. (선택) 카카오 알림** — `node scripts/kakao-auth.js` 실행해서 나오는 안내 따라하기 (15분)
- [ ] **7. 최종 점검** — `npm run doctor` 실행 → 전부 ✅면 끝. 또는 Actions에서 autopost를 mode `test`로 수동 실행
- [ ] **8. 끝.** 다음 스케줄 날짜 11:00에 첫 게시물이 자동 발행됩니다

## 오픈 후 루틴 (주 1회, 5분)

- [ ] 관제실( https://leejiho-pslab.github.io/snsautomatic/ ) 접속 → 기획안 카드 클릭해서 다음 발행분 검수
- [ ] 고칠 게 있으면 카드의 **✏️ 수정요청** 버튼 (또는 클로드에게 직접 요청)
- [ ] 콘텐츠가 2건 이하로 남으면 클로드에게: **"동성인쇄소 다음 콘텐츠 만들어줘"**

## 실제 정보로 교체하면 좋은 것 (콘텐츠 품질)

- [ ] CTA 슬라이드·caption의 전화번호(현재 `0507-0000-0000` 자리표시자) → 실제 번호
- [ ] 카카오채널명 `@동성인쇄소` → 실제 채널명
- [ ] 인스타 핸들이 `@dongsung_print`가 아니면 → `clients/dongsung-print/config.json`에서 수정
- [ ] 7/16 "카페 메뉴판 사례" — 실제 작업 사진이 있으면 보내주세요, 슬라이드에 반영해 드립니다
